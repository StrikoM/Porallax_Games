using UnityEngine;

// Этот скрипт был деактивирован, так как он несовместим с URP.
// Эффект CRT теперь работает через UI Overlay (CRT_Overlay_Safe).
public class CRTManager : MonoBehaviour 
{ 
    void Start() { Debug.Log("CRTManager деактивирован. Используется новый метод оверлея."); }
}
